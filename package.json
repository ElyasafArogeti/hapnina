{
  "name": "hapnina",
  "version": "1.0.0",
  "main": "server/my_server.js",
  "scripts": {
  "start": "node server/my_server.js",
  "postinstall": "cd server && npm install && cd ../client && npm install",
  "build": "cd client && npm run build"
}
,
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "mysql2": "^3.12.0"
  }
}
